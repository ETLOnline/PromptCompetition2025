import express from "express";

const router = express.Router()

// All authentication routes are now handled by Clerk on the frontend
// This file is kept for potential future backend-only auth endpoints

export default router
